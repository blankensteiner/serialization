namespace Serialization.MicrosoftJson.Tests
{
    public enum Int8Enum : byte
    {
        Default = 0,
        Max = byte.MaxValue
    }

    public enum UInt8Enum : sbyte
    {
        Default = 0,
        Max = sbyte.MaxValue
    }

    public enum Int16Enum : short
    {
        Default = 0,
        Max = short.MaxValue
    }

    public enum UInt16Enum : ushort
    {
        Default = 0,
        Max = ushort.MaxValue
    }

    public enum Int32Enum : int
    {
        Default = 0,
        Max = int.MaxValue
    }

    public enum UInt32Enum : uint
    {
        Default = 0,
        Max = uint.MaxValue
    }

    public enum Int64Enum : long
    {
        Default = 0,
        Max = long.MaxValue
    }

    public enum UInt64Enum : ulong
    {
        Default = 0,
        Max = ulong.MaxValue
    }
}
